# OCR 截图转文字工具 — PRD v1.2

## 1. 产品目标和使用场景

### 产品目标
一个轻量级 Mac 桌面工具：用户截图后，点击上传或 Cmd+V 粘贴截图，工具自动识别并显示文字，支持一键复制到剪贴板。

**交付形态**：Flask Web 应用，启动后自动打开浏览器，功能即用。

### 使用场景
- 从 PDF / 网页 / 视频截图提取文字
- 从图片中快速摘录中文 / 英文内容
- 日常办公中遇到无法直接复制的文字时，截图→粘贴→得文字
- 本地图片文件拖入或上传批量单张识别

### 核心用户
Mac 用户，中文场景为主，追求轻量快速。

---

## 2. 整体页面框架和导航结构

单页面 Web 应用，无需页面跳转。页面结构如下：

```
┌───────────────────────────────────────────────────┐
│  标题：OCR 截图转文字                                  │
│  副标题：上传或粘贴截图，自动识别文字                     │
├───────────────────────────────────────────────────┤
│  [⬆ 上传图片]  或直接在下方区域 Cmd+V 粘贴 / 拖拽图片    │  ← 工具栏
├───────────────────────────────────────────────────┤
│                                                    │
│  ┌──────────────────────────────────────────────┐  │
│  │                                               │  │
│  │          大型粘贴区（占据主要面积）                │  │
│  │          按 Cmd+V 粘贴截图到此处                  │  │
│  │          支持拖拽图片到此处                       │  │
│  │                                               │  │
│  │          [粘贴后：显示截图预览]                   │  │
│  │                                               │  │
│  └──────────────────────────────────────────────┘  │
│                                                    │
│  ┌──────────────────────────────────────────────┐  │
│  │  识别结果                    [xxx 字]           │  │
│  │  ─────────────────────────────────────────    │  │
│  │  识别出的文字内容…（可选中、可编辑、可滚动）        │  │
│  └──────────────────────────────────────────────┘  │
│                                                    │
│                            [清除]  [复制到剪贴板]      │
├───────────────────────────────────────────────────┤
│  底部状态栏：就绪 / 识别中… / 识别完成 / 错误提示       │
└───────────────────────────────────────────────────┘
```

**交互流程**：
1. 上传或粘贴截图 → 自动触发识别
2. 识别结果显示在下方区域
3. 点击「复制到剪贴板」或手动选中 Cmd+C

---

## 3. 首页（主界面）内容结构

### 3.1 工具栏（最上方，图片区域上方）
- **上传图片按钮**：蓝色主按钮，带上传图标，点击弹出访达（Finder）文件选择器
  - 接受格式：PNG / JPG / GIF / BMP / WebP
  - 选择文件后自动替换当前图片并触发识别
  - 支持重复选择同一文件（输入框 value 自动清空）
- **提示文字**：按钮右侧灰色小字「点击左侧按钮在访达中选择文件，或直接在下方区域 Cmd+V 粘贴 / 拖拽图片」

### 3.2 图片粘贴区（页面主体上半区，大面积，flex: 1 占满剩余空间）
- **行为约束（关键）**：
  - ❌ 点击该区域**不弹出**文件选择器
  - ❌ 不作为上传按钮使用
  - ✅ 仅支持两种交互：Cmd+V 粘贴 + 拖拽文件
- **视觉标识**：
  - 虚线边框（2px dashed），hover 时颜色微微加深
  - cursor: default（鼠标悬停不显示为可点击手型）
  - 无 tabindex，不可聚焦
- **初始状态**：灰底居中显示占位图标 + 「按 Cmd+V 粘贴截图到此处」
- **粘贴后**：显示截图，自适应容器等比缩放（max-height: 100%）
- **拖拽支持**：拖入时边框变实线蓝色，背景变浅蓝
- **重复操作**：新图片覆盖旧图片，自动触发重新识别

### 3.3 识别结果区（页面下半区）
- **标题**：左侧「识别结果」，右侧显示字数统计
- **初始状态**：空白，显示浅灰占位文字「识别结果将显示在这里…」
- **识别中**：清空旧结果，状态栏显示进度
- **识别完成**：可编辑的纯文本区域，自动换行，支持滚轮滚动
- **识别失败**：状态栏红色显示错误信息
- 结果区域可手动编辑，方便用户修正 OCR 结果后复制

### 3.4 操作按钮（结果区下方，右对齐）
- **复制到剪贴板**：主按钮（蓝色），点击复制结果文字到系统剪贴板
  - 点击后文字短暂变为「已复制 ✓」（1.5 秒后恢复）
  - 无识别结果时按钮置灰不可点击
  - 失败时 fallback：选中文字提示手动 Cmd+C
- **清除**：次要按钮（红色边框），清空图片和结果，恢复初始状态

### 3.5 底部状态栏
- 居中显示当前状态文字
- 状态类型：
  - 就绪（灰色）：显示「就绪 — 按 Cmd+V 粘贴截图」
  - 识别中（蓝色 #spinner 转动）：显示「正在识别…」
  - 完成（灰色）：显示「识别完成 — xxx 字」
  - 错误（红色）：显示具体错误信息

---

### 附录：技术决策

| 项目 | 选择 |
|---|---|
| 语言 | Python 3.9+ |
| 界面框架 | Flask + 原生 HTML/CSS/JS（Web 浏览器） |
| OCR 引擎 | Surya 0.5.0 |
| 输入方式 | 上传按钮 + Cmd+V 粘贴 + 拖拽文件 |
| 启动方式 | 双击 .app 或 .command 文件，自动打开浏览器 |
| 核心功能 | 上传/粘贴→自动识别→一键复制 |
| 首版不做 | 导出文件、历史记录、批量识别、全局快捷键截图 |
